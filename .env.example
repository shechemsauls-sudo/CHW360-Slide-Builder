# =============================================================================
# Supabase — CHW360 Slide Builder
# =============================================================================

# Database Connections (Supabase PostgreSQL)
# Use TRANSACTION POOLER for serverless/Next.js (port 6543)
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-1-[region].pooler.supabase.com:6543/postgres"

# Direct connection (for migrations, Drizzle Studio — port 5432, no pooling)
DATABASE_URL_DIRECT="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"

# Session pooler (persistent connections — port 5432, pooled)
DATABASE_URL_SESSION="postgresql://postgres.[project-ref]:[password]@aws-1-[region].pooler.supabase.com:5432/postgres"

# Supabase Password
SUPABASE_DB_PASSWORD=""

# Supabase API
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""

# Supabase Service Role Key (server-side only — DO NOT expose to client)
SUPABASE_SERVICE_ROLE_KEY=""

# Supabase MCP Server
SUPABASE_MCP_URL=""

# App
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
NODE_ENV="development"
